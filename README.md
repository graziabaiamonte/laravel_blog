### Simple Blog with Laravel

Ogni volta che il browser chiede una pagina, Laravel fa sempre lo stesso percorso:
Browser  →  routes/web.php  →  Middleware  →  Controller  →  Model (DB)  →  View  →  Browser
